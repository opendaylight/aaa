<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.hp</groupId>
    <artifactId>hp-util-model-persistence-root</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>hp-util-model-persistence-cassandra</artifactId>
  <packaging>jar</packaging>

  <name>hp-util-model-persistence-cassandra</name>
  <description>Persistence implementation using Cassandra</description>

  <dependencies>
    <dependency>
      <groupId>com.hp</groupId>
      <artifactId>hp-util-model-persistence</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <!-- http://mvnrepository.com/artifact/com.netflix.astyanax/astyanax/ -->
      <groupId>com.netflix.astyanax</groupId>
      <artifactId>astyanax</artifactId>
      <version>${astyanax.version}</version>
      <exclusions>
        <!-- Newer versions to 1.56.18 of Astyanax has a reference to JPA 1.0 (javax.persistence) which has a conflict with Hibernate which references JPA 2.0 -->
        <!-- Exclude if hp-util-model-persistence-jpa is also used. -->
        <!-- 
        <exclusion>
          <groupId>javax.persistence</groupId>
          <artifactId>persistence-api</artifactId>
        </exclusion>
        -->
      </exclusions>
    </dependency>
  </dependencies>

</project>
